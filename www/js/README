jQuery UI: using datepicker, autocomplete
for elFinder: dialog, tabs, selectable, draggable, droppable, resizable

jqgrid: Grid base (jqGrid), Formatter, Custom (additional methods), Table to Grid, jQuery UI addons
